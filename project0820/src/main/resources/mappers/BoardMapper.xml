<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.project0820.BoardMapper">

    <select id="findAll" resultType="Board">
        SELECT id, author, title, content FROM board
    </select>

    <select id="findById" parameterType="long" resultType="Board">
        SELECT id, author, title, content FROM board WHERE id = #{id}
    </select>

    <insert id="save" parameterType="Board" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO board (author, title, content)
        VALUES (#{author}, #{title}, #{content})
    </insert>

    <update id="update" parameterType="Board">
        UPDATE board
        SET author = #{author}, title = #{title}, content = #{content}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM board WHERE id = #{id}
    </delete>

</mapper>