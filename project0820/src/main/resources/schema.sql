CREATE TABLE board (
                       id BIGINT AUTO_INCREMENT PRIMARY KEY,
                       author VARCHAR(255),
                       title VARCHAR(255),
                       content VARCHAR(255)
);